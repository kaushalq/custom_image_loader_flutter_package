name: custom_loading_loader
description: "A highly customizable Flutter loading indicator — orbit dots (or custom widgets) around a central image with full control over shape, color, count, speed, curve, pulse, and more."
version: 2.0.2
homepage: https://github.com/kaushalq/custom_image_loader_flutter_package

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
